import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      light: "rgba(122, 235, 52, 100%)",
      main: "rgba(122, 235, 52, 93%)",
    },
    background: {
      paper: "rgba(34,38,35,93%)",
      default: "rgba(83, 200, 60, 1)",
    },
  },
});
